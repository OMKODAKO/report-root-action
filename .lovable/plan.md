## Changes

### 1. Remove duplicate logo from hero (`src/routes/index.tsx`)
The Navbar already shows the 72px logo. Remove the extra `<Logo size={72} />` + "Take Care" label block that sits directly above the "Small Actions. Visible Change." headline. Keep the headline, subcopy, and CTAs.

### 2. Redesign the logo (`src/components/Logo.tsx`)
Replace the current flat SVG with a more refined version while keeping the same "flower growing through cracked concrete" concept and the 72px default size:
- Soft circular background badge with a subtle green→white gradient so the mark reads as a polished app icon
- Layered concrete slab with a more natural jagged crack (polyline, not a single stroke)
- Curved stem with a small highlight, two leaves of different sizes for balance
- Flower with 6 evenly-rotated petals + soft drop-shadow filter and a warm amber center
- Subtle inner shadow on the badge for depth
- Pure SVG, no extra deps; remains crisp at any size; `aria-label` preserved

### 3. Add Google sign-in to signup + login (`src/routes/signup.tsx`, `src/routes/login.tsx`)
- Enable managed Google OAuth via `configure_social_auth` with `providers: ["google"]` (keep email enabled)
- Add a "Continue with Google" button above the email/password form on both pages, separated by an "or" divider
- Use `lovable.auth.signInWithOAuth("google", { redirect_uri: window.location.origin })` from `@/integrations/lovable` (generated by the configure tool); handle `result.error` with toast, return on `result.redirected`, otherwise navigate to `/dashboard`
- Google icon as inline SVG (no new dependency)

### 4. Quietly fix hero hydration warning
The hero stat cards render server-side values that briefly mismatch on hydration. While editing `index.tsx`, ensure stat numbers render the same on server and client (use the loader data directly, no `Date.now()` / locale formatting in the tree).

## Out of scope
- No changes to map, feed, dashboard, admin, report flow, or DB schema
- No changes to navbar logo size (stays 72px as requested earlier)
